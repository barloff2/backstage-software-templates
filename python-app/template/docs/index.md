# Documents for ${{values.app_name}}

This application has two endpoints:

 - `${{values.app_name}}-${{values.app_env}}.test.com/api/v1info`
 - `${{values.app_name}}-${{values.app_env}}.test.com/api/healthz`

Here you could expand on what each of these endpoinst do.

# How to access the app?

You can access to app by accessing this URL: `${{values.app_name}}-${{values.app_env}}.test.com/api/healthz`